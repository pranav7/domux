[package]
name = "domux-term"
description = "Terminal emulator trait and implementation for domux"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
links = "ghostty-vt"
build = "build.rs"

[lints]
workspace = true

[dependencies]
bitflags = { version = "2.13.1", features = ["serde"] }
compact_str = "0.10.0"
schemars = "1.0"
serde = { version = "1", features = ["derive"] }

[build-dependencies]
sha2 = "0.10"
toml = "0.9"

[dev-dependencies]
clap = { version = "4.6.6", features = ["derive"] }
criterion = { version = "0.8.2", default-features = false }
portable-pty = "0.9.0"
sha2 = "0.10"
toml = "0.9"

[[bench]]
name = "feed"
harness = false
