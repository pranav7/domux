# Third-party licenses

The domux binary is compiled from the Rust crates below. Each section names one license,
the crates that use it, and reproduces the license text. Generated by cargo-about from
about.toml; see NOTICE for the Ghostty library compiled in.

## Summary

{{#each overview}}
- {{name}} ({{id}}): {{count}}
{{/each}}

{{#each licenses}}
## {{name}} ({{id}})

Used by:

{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} ({{crate.repository}}){{/if}}
{{/each}}

```text
{{text}}
```

{{/each}}
